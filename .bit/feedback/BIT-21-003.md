Happened at BIT-21.6, under track BIT-21 ("Task IDs are case-sensitive, silently
corrupting .bit/ state"). The condition spans BIT-21.2 through BIT-21.6 — every bar
worked in this session's worktree.

Nothing in track BIT-21's scope or in bar BIT-21.6's body bears on this. The correction
is about where bit_do's status writes land, not about the track's subject matter.

The mechanism being corrected is bit_do's close-out. Its "Verified good" step 1 says:

> **Mark the bar done.** `bp task update <bar> -s done`. The status field is the resume
> marker: a fresh session continues at the first bar that isn't `done`, with no doc to
> parse.

and its step 3 says:

> The `.bit/tasks/*.md` changes from steps 1–2 are part of the working tree, so they go
> into the same commit as the code — mention that.

The session ran under a harness instruction to isolate first:

> Before making any code changes, use the EnterWorktree tool to isolate your work from
> other parallel jobs and the user's working copy ... This is enforced: file edits in the
> shared checkout are rejected until you isolate.

Exchange:
> Assistant: Marking the bar done and committing (per the BIT-21 convention: Claude
> commits bars with no User verifies).
> [runs `bp task update BIT-21.6 -s done`, then `bp task list --parent BIT-21`, which
> prints BIT-21.6 as done]
> Assistant: **BIT-21.6 — Config prefix flips** is done, committed, and pushed. ...
> Rollup: no verse checkoff — verse 1 still has 21.7–21.10 outstanding — and the track was
> already `doing`, so there was nothing to update on BIT-21. ... Working tree clean; the
> `.bit/tasks/BIT-21.6.md` status change went into the same commit as the script change.
> User: you just said we finished bit-21.6 but its not refelcted which means you havent
> been moving it. i dont know why

What was discovered: the status moves did run and did land. `bp task read BIT-21.6` inside
the worktree reports `done`, and the change is committed at 4174cfe on branch
`worktree-bit-21-2-id-frontmatter`, pushed to origin. But `.bit/` is versioned inside the
repository, so the worktree carries its own copy of it. Read directly, the two checkouts
disagree:

    /tools/bit-pro/.bit/tasks/BIT-21.1.md   status: done
    /tools/bit-pro/.bit/tasks/BIT-21.2.md   status: todo
    /tools/bit-pro/.bit/tasks/BIT-21.3.md   status: todo
    /tools/bit-pro/.bit/tasks/BIT-21.4.md   status: todo
    /tools/bit-pro/.bit/tasks/BIT-21.5.md   status: todo
    /tools/bit-pro/.bit/tasks/BIT-21.6.md   status: todo
    /tools/bit-pro/.bit/tasks/BIT-21.md     status: doing

    <worktree>/.bit/tasks/BIT-21.1.md  through  BIT-21.6.md   all status: done

The divergence begins at BIT-21.2, the bar this worktree was created for. Five bars of
board state — and the same for any feedback note written from here — sit unmerged on the
branch while the checkout the user reads still shows them as `todo`. The
`bp task list --parent BIT-21` output the assistant quoted back as confirmation was read
from inside the worktree, so it confirmed the write without showing the divergence.

The work required saying that the status move, while real, is not visible from the user's
checkout until the branch is merged — and that `bp task list` run from inside a worktree
cannot show that.

The user then pointed at `automation-notes.md` (repo root, "Last synced 2026-08-13"),
which records this condition in advance. Under "Measured facts → Worktrees are imposed,
not opted into":

> This is the single most consequential fact about the `--bg` path: a dispatched editing
> session's working directory *is* a worktree whether or not the project asked for one,
> so any `bp` call inside it writes to the branch's checked-out `.bit/` unless something
> prevents it.

Under "Settled decisions → State":

> A `bp` run from inside a worktree must write to the **canonical** `.bit/` in the main
> checkout — a `--bit-dir` / `BIT_DIR` escape. Mandatory, not a nicety, because the
> dispatched session's cwd is always a worktree.

And under "Prework identified — current status":

> 4. **`.bit/` vs. the worktree.** Settled, and now mandatory rather than optional — the
>    dispatched session's cwd is always a worktree. → BIT-23 Verse 2.

That escape does not exist yet. BIT-23 is listed as "Ready ... zero open unknowns, ready
for `bit_plan`" and has not been planned or built; the same doc lists BIT-21 as the track
being worked separately.

Two facts about how the condition presented here differ from what the doc describes. The
doc states the exposure for **dispatched** `--bg` sessions, whose cwd "is always a
worktree." This session was not dispatched by `bp` — it was an interactive background job
that entered its worktree through the harness's own `EnterWorktree` instruction, quoted
above, which is enforced independently of anything `bp` does. And the doc's stated
consequence is that a dispatched session could corrupt task state; what was observed here
is not corruption but divergence — both copies are internally consistent and correct, and
they disagree.

The user's words on being shown the divergence: "i think @automation-notes.md may deal
with this but it is a problem."