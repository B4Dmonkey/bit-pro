Addendum to BIT-23-003: the BIT_DIR problem is not limited to bp task update. Every bp subcommand — including bp feedback add — writes to whatever .bit/ is local to the cwd. The feedback note BIT-23-003 itself landed in the worktree's .bit/feedback/, not main's. The canonical store is not reachable by any bp call made from inside a worktree without explicit BIT_DIR routing. The fix needs to cover all commands, not just task writes.