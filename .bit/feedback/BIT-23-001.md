Happened during bit:scope work on BIT-23 (scope refinement, no bar — the write-back step).

bit:scope's write-back step wrote the multi-line scope body to a temp file using a Bash heredoc:

    cat << 'BODY' > /Users/appstack/.claude/jobs/bdb210f0/tmp/bit23-body.md
    [body content]
    BODY

This triggered a permission sub-prompt requiring user approval before it ran.

Exchange:
> User: you just asked for a shell command that had a sub approval. i dont want to approve that command i want you to just do it. we need to figure out a way for you to achieve the same result without my approval.

The work required writing the scope body to a temp file without triggering a permission sub-prompt.