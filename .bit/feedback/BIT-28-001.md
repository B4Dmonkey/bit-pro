Happened during bit_plan work on BIT-28 ("daemon lifecycle (serve / start /
stop / status)"). No bar exists yet — this surfaced before any bar was
created.

Scope (BIT-28) has no `## Risks & unknowns` section at all. Its body carries
`## Why`, `## Summary`, `## Visual aid`, `## Decisions` (18 entries),
`## Verses` (2), and `## References`. `bp task list` shows the track's
approved column empty:

    BIT-28\ttodo\tdaemon lifecycle (serve / start / stop / status)\t\t

bit_plan's opening gate is what produced the verdict to proceed. It reads:

> ## First: is anything still open?
> Before you research anything, before you draft a single bar — in either
> mode — read the scope's `## Risks & unknowns` section. If it has entries,
> **stop and ask the user whether to plan at all.**

and

> **An empty or absent Risks section is not proof that nothing is open.** It
> means bit_scope didn't find anything, which is a weaker claim.

The gate names only the Risks section as the thing to read. It says nothing
about the track's approval flag. `bp instructions` documents approval as the
scope sign-off: "a track approved means the scope is blessed and planning may
proceed; every bar approved means the plan is blessed and work may proceed."

Exchange:
> Assistant: No `## Risks & unknowns` section — the scope carries Decisions
> instead. Let me verify that and research the codebase before drafting.
> [ran `bp task list --parent BIT-28`, `ls cmd/`, read `cmd/root.go` and
> `cmd/cmd_test.go`, began reading `automation-notes.md`]
> User: you failed the test. there are no risk & unknowns but the scope has
> not been approved.

The work required checking BIT-28's approval flag before researching or
drafting. The track is unapproved, so planning should not have started. What
was actually checked was the absence of a Risks section, which the gate
treats as the sole precondition; the empty approved column in the
`bp task list` output was read past.