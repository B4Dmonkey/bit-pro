Happened during bit_do on BIT-21.14, under track BIT-21 ("Task IDs are case-sensitive,
silently corrupting .bit/ state"). BIT-21.14 was already done and committed at the time;
verse 2 still had BIT-21.15, .16 and .17 outstanding.

Scope (BIT-21) Summary says:
> Make uppercase the one spelling a task ID ever has. Normalize the existing projects on
> disk once, then normalize at every boundary the CLI reads from — the ID a caller passes,
> the ID stored in a task's frontmatter, and the prefix in `config.toml` — so no later
> input, hand-edit, or freshly-initialised project can put a lowercase ID back into
> circulation.

Scope (BIT-21) Decision, as it read at the time:
> **The fix lives in `task/`, not at the CLI boundary.** Seven commands take an ID and
> every one hands it to a `Store` method. `Store` is the one choke point they all pass
> through, and `Path` is the single function that turns an ID into a filename — normalizing
> there covers every command, including ones not yet written. Confirmed while planning:
> every ID, prefix and track argument in `cmd/` and `tui/` reaches disk through a
> `task.Store` method, so `cmd/` needs no ID handling of its own.

Scope (BIT-21) Verse 2 says:
> Touches: the ID and path helpers in `task/store.go` (`Path`, `Load`, `Save`, `children`,
> `NextID` / `NextChildID`, the order helpers), the note write in `task/feedback.go`, the
> prefix in `task/config.go`, and `cmd/init.go` — where to look to verify.

`cmd/task_list.go` is not among those.

Bar BIT-21.14's Scope says:
> - `task/task.go` — normalize `ID` in `Parse`, so every task reaches the rest of the code
>   canonical regardless of what its frontmatter says.
> - `cmd/task_complete_test.go`, `cmd/task_update_test.go` — the new tests.

Exchange (this surfaced while measuring bit_do's own token usage after BIT-21.14 closed out,
not while implementing the bar):
> Assistant: That's a finding I need to chase before anything else: `bp task list --parent
> bit-21` returns zero bars, exit 0 — while `bp task read bit-21` resolves fine. [...] That
> matters twice over: it kills #2 as I wrote it (your invoke *is* lowercase — bit_do would
> conclude the track has no plan), and it contradicts a decision in BIT-21's own scope.
> User: dont worry about the bug im fairly certain thats what this track is about
> Assistant: [checked the remaining bars and reported that BIT-21.15's scope is
> `task/task.go` — normalize each `Order` entry in `Parse` — plus `cmd/task_list_test.go`,
> so its fix is on the `Parse` side, while the raw side of the `--parent` comparison is the
> flag value off the command line; .15 won't reach it]
> User: yea do it just make sure you use the bitplan skill to fix it

What was discovered: `cmd/task_list.go:26` filters with
`strings.HasPrefix(t.ID, parent+".")` using the raw `--parent` flag value, so
`bp task list --parent bit-21` returns zero bars with exit code 0. It is the only
`HasPrefix` filter on an ID in `cmd/` and `tui/`. `t.ID` was already canonical at that point
— BIT-21.14 had shipped — so the unnormalized side was the flag value, which never reaches
disk and so never reaches a `Store` method. BIT-21.15 (`Order` entries in `Parse`), .16
(feedback note filenames) and .17 (the `config.toml` prefix) do not cover it.

The work required correcting the scope Decision quoted above (commit 3bc532f) and adding a
new bar, BIT-21.18, closing the gap by exporting `Store.Children` over the private
`children` rather than adding ID handling to `cmd/` (commit bc039d5).
