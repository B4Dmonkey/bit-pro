Happened during bit_plan work on BIT-28 ("daemon lifecycle (serve / start /
stop / status)"). No bar exists yet — this surfaced before any bar was
created. BIT-28 was approved by this point, and has no `## Risks & unknowns`
section.

Scope (BIT-28), Verse 1, in full:

> - [ ] Verse 1 — Operator can run the daemon in their terminal and watch it: `bp serve` runs the stub
>   loop attached to the terminal, logging each tick, and exits cleanly on Ctrl-C / `SIGTERM`. Nothing
>   launchd-related yet — this is the walking skeleton the plist will later point at, and it fails
>   cheap if the body can't run at all.
>   Touches: `cmd/serve.go` (new), `cmd/root.go` (register it) — where to look to verify.

Its Visual aid says of the same command:

> bp serve   ─▶ the daemon itself: runs the loop in the foreground, logs each tick,
>              exits cleanly on Ctrl-C / SIGTERM. What the plist points at, and how an
>              operator watches the loop live instead of tailing a log.

Neither names a tick interval or the text a tick logs. `grep -niE
"tick|interval|poll|every [0-9]|second" automation-notes.md` returns no
interval either; the closest is line 22, `*Done when:* it ticks in the
terminal and exits cleanly on Ctrl-C.`

For the operator surface, the scope fixes `bp status`'s output exhaustively:

> - **`bp status` reports three states, not two: `running`, `not running`, `stopped`.**

and its Visual aid fixes exactly one `bp start` line, the already-running case:

> bp start   ─▶ plist missing?      → write it
>              launchctl enable gui/$UID/<label>      (clears a previous bp stop)
>              not loaded?          → launchctl bootstrap gui/$UID <plist>
>              loaded, not running? → launchctl kickstart gui/$UID/<label>
>              running?             → "already running (pid N)"

The three `bp start` paths that do start something, and every `bp stop`
path, carry no output text. The `bp stop` Visual aid entry is commands only:

> bp stop    ─▶ launchctl bootout  gui/$UID/<label>      (unloads + kills, this session)
>              launchctl disable gui/$UID/<label>      (persists: stays down after reboot)

Separately, the scope's Decisions state the `bp stop` ordering as settled:

> - **`bp stop` is durable; an unstopped daemon restarts itself after a reboot.** Stopping is an
>   explicit operator intent that outlives the login session, so `bp stop` is `bootout` *and*
>   `disable`

while `automation-notes.md:187` — which the scope's own References section
names as "the authority for the launchd mechanics" — withholds it:

> The `bootout`/`disable` ordering within `bp stop` is unverified — settle it with a throwaway label at
> plan time, not by reasoning about it.

`automation-notes.md:27` reads: `BIT-28's scope is revised for launchd and
has no open unknowns — ready for bit_plan.`

bit_plan's own gate is what produced the verdict to stop. Its opening
section says an absent Risks section is a weaker claim than "nothing is
open," and keeps the gate live through drafting:

> **An empty or absent Risks section is not proof that nothing is open.** It
> means bit_scope didn't find anything, which is a weaker claim. So the gate
> stays live through drafting: the moment writing a bar requires you to
> settle something the scope didn't, stop *there* — before that bar exists —
> and either hand back to bit_scope or ask. In practice it looks like:
>
> - **A user-facing string the scope never fixed** — a label, a command
>   name, an error message. You need one to write the test's assertion, and
>   choosing it makes the plan the author of a wording call. That's a scope
>   Decision (Trap 1) — hand it back.

Exchange:
> Assistant: Stopping before I draft a bar. The gate is clear on approval and
> Risks, but four things the scope didn't settle are needed to write the bars,
> and each one would make the plan the author of a decision.
> **1. `bp serve`'s tick interval.** ... **2. What a tick logs.** ... **3. What
> `bp start` prints on success.** ... **4. What `bp stop` prints.** ...
> Separately — not a decision, a verification: `automation-notes.md:187` says
> [the ordering is unverified]. The scope's Decision states bootout-then-disable,
> so it reads settled, but the notes explicitly withhold that. Settling it means
> bootstrapping and booting out a throwaway LaunchAgent on your machine — I'm not
> doing that unprompted.
> Recommend handing 1–4 back to `/bit:scope` as Decisions.
> User: yes lets hand back to /bit:scope , also for /bit:feedback this is a
> perfect example of bit:scope being incomplete and you passing back to scope.
> its a good example of what should be done

The work required stopping before any bar was created and handing the four
items back to bit_scope. What was checked was the scope body end to end, its
References target `automation-notes.md`, the four candidate `cmd/` files
(none exist yet), and `grep` over `automation-notes.md` for a tick interval.
Nothing was assumed and no bar was written.