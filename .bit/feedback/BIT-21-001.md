Happened during bit_plan on track BIT-21 ("Wrong-case task IDs silently corrupt .bit/
state"), between the scope edit and the creation of bars BIT-21.1 – BIT-21.17.

Scope (BIT-21) before the edit carried this Decision:

> - **The migration must cover all four ID carriers.** Filename, `id:` frontmatter,
>   `order:` lists, and the `config.toml` prefix. Body prose and git commit messages
>   that cite `BIT-11.4` are deliberately left alone: nothing in the code parses them,
>   so a stale citation is cosmetic. Case-only renames need `git mv --force` on a
>   case-insensitive filesystem.

bit_plan's gate directs an unsettled question back to bit_scope rather than settling it
in the plan. Its text:

> **An empty or absent Risks section is not proof that nothing is open.** It means
> bit_scope didn't find anything, which is a weaker claim. So the gate stays live
> through drafting: the moment writing a bar requires you to settle something the scope
> didn't, stop *there* — before that bar exists — and either hand back to bit_scope or
> ask.

and, on ownership:

> If they agree, hand back to bit_scope to author the split (it owns tracks; you own
> bars), then plan each track.

The gate fired as designed: four questions were put to the user with AskUserQuestion
before any bar was written (feedback note filenames as a fifth ID carrier; the script's
invocation shape; the instructions filename; whether the migration script is
test-driven), and the user answered all four. What followed is what the correction is
about — bit_plan then rewrote the BIT-21 track body itself rather than invoking
bit_scope, and created all 17 bars in the same uninterrupted run.

The rewrite replaced the four-carrier Decision quoted above with a five-carrier version,
added two new Decisions (`git mv --force` measured; the script's invocation and bash
harness), added a fourth symptom to the Why, added a second observed block to the Visual
aid, and reworded both verses' "Touches" lines.

Exchange:

> Assistant: Fourth symptom, worse than the three the scope lists: `bp task create
> --parent bit-1` **silently destroyed an existing bar**. `BIT-1.1.md` kept its filename
> but its contents are now a different task with `id: bit-1.1` — the original bar is
> gone, exit 0, no output.
>
> Updating the scope first with the four things you settled plus this measurement, then
> writing the bars.

> Assistant: Scope updated and round-trips clean. Two quick checks so the bars only name
> commands that actually work here:

> Assistant: All 17 bodies drafted. Creating the bars under BIT-21 in delivery order:

> User: if planning needs to update the scope. we dont automatically write the bars
> after. i need to review them first. also if planning needs to update the scope we
> should call the scope skill

The work required two things that did not happen: the scope update should have gone
through the bit_scope skill rather than being written by bit_plan directly, and bar
creation should have stopped after the scope update for the user to review it, rather
than continuing into all 17 bars in the same run. The user reviewed the scope rewrite
and the 17 bars only after both were already written to `.bit/`.