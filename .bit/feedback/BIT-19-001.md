Happened during bit_plan on track BIT-19 ("Stop-hook reminder for uncommitted bar
close-outs"). No bar yet — BIT-19 has no bars; this was a create-mode planning pass,
invoked as `/bit:plan bit-19`.

Scope (BIT-19) carries a populated `## Risks & unknowns` section with two entries, the
first flagged to be resolved before planning:

> - **Unknown:** `bit_do` never defines a fixed, mechanically-checkable shape for "the
>   suggested commit message" — right now it's freeform prose, so a hook has nothing
>   reliable to grep for.
>   **Resolve by:** decide a minimal literal marker `bit_do` must use going forward (e.g. a
>   fenced block introduced by a fixed line like "Suggested commit:"), so the hook's check
>   and `bit_do`'s prose agree on the same shape.
>   **De-risk before planning?** Yes — this decision shapes both the hook's detection logic
>   and a companion edit to `bit_do`'s close-out section; get it settled before bit_plan
>   writes steps for either.
>
> - **Unknown:** false positives vs. false negatives — a bar can legitimately sit `doing`
>   for reasons unrelated to the current reply (e.g. genuinely awaiting a separate
>   User-verifies confirmation from turns ago), so "any doing bar" is too broad a trigger.
>   **Resolve by:** decide the tolerance now rather than mid-plan — this feature exists to
>   catch a forgotten reminder, so an occasional unnecessary nudge is cheap; silently never
>   reminding is the actual failure mode being fixed. Bias detection toward reminding too
>   often over too rarely.
>   **De-risk before planning?** No — this is a stance to state as a Decision, not something
>   that needs a spike to resolve.

Neither unknown is attached to a verse marked `(spike)`; BIT-19's three verses carry no
spike marker.

bit_plan's intake step names the risks only as something to read, with no gate on them:

> **Start from the scope track.** The user names it (by ID or title); read its body end to
> end with `bp task read <track> --body` — the WHY, the verses, the "touches" pointers, the
> risks, and the References section if one exists.

and immediately pushes toward planning everything:

> Default to planning every verse in one pass. Splitting into multiple planning sessions
> exists to route around a genuine unknown, not as a default posture just because a scope
> has more than one verse. If the scope is clear and nothing is open, plan it end to end
> now; don't ask the user to pick a verse to be cautious.

The skill's stop-and-confirm machinery is scoped to spikes specifically — "When the scope
has spike verses", "**Plan every spike.**", "Later verses depend on the answer → plan the
spikes and nothing else" — and BIT-19's unknowns are not spikes, so none of it engaged.

What the session actually did before raising the unknowns: read `bp instructions`, listed
tasks, read the BIT-19 body, read the repo layout (`bit/`, `Justfile`, `go.mod`,
`plugin.json`, `marketplace.json`, `install.sh`, `cmd/`), grepped `bit/skills/do/SKILL.md`
for the commit prose, and fetched the Claude Code hooks documentation to verify the `Stop`
event's stdin fields, the `hookSpecificOutput.additionalContext` shape, and the plugin
`hooks/hooks.json` structure. Only then was the open unknown reported, in the same message
as two proposed design questions (the marker's literal shape, and whether the hook is a
`bp` subcommand or a shell script).

Exchange:
> Assistant: Read BIT-19 and verified its three Decisions against the current hooks docs —
> `Stop` does receive `last_assistant_message`, non-blocking
> `hookSpecificOutput.additionalContext` is real, and a plugin's `hooks/hooks.json` with
> `${CLAUDE_PLUGIN_ROOT}` is the right shipping mechanism. So the scope's approach holds.
> There are no bars under BIT-19 yet, so this is a fresh plan.
>
> Two things block writing steps, though:
>
> **The scope's own open unknown is still open** — it's flagged `De-risk before planning?
> Yes`, and Verse 2 says the marker convention is "decided above" when it isn't. That
> literal string is the hook's entire detection logic *and* the edit to `bit_do`'s
> close-out, so a plan written before it's settled would be guessing at its own acceptance
> criteria.
>
> **The scope doesn't say what the hook *is*.** It says "a new hook script under
> `bit/hooks/`", but this repo's only test harness is `go test ./...`. A shell script has
> nothing to test-drive it, which collides with the TDD-first requirement on every bar.
>
> The second unknown in the scope (false positives vs. negatives) is already answered in
> its own text — "bias toward reminding too often" — so I'll move it into Decisions rather
> than leave it sitting in Risks.
>
> [then presented two AskUserQuestion questions: the marker's literal shape, and whether
> the hook is a `bp` subcommand or a POSIX shell script]
>
> User: [rejected the questions] The user wants to clarify these questions.
>
> Assistant: What would you like to clarify? / For reference, the two things I'm stuck on
> are: 1. **The marker** … 2. **What the hook actually is** …
>
> User: there are unknowns in the scope /bit:feedback when we start planning the first
> thing we should check is if there are unresolved risk and unknowns and stop planning to
> get confirmation from the user that we should even plan

The work required checking for unresolved risks and unknowns as the first action of a
planning pass, and stopping there to get the user's confirmation that planning should
happen at all — rather than researching the codebase and the external documentation first
and surfacing the open unknown alongside proposed answers to it.