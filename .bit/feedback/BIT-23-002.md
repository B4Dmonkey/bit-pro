Happened during bit:do execution of BIT-23.2, under track BIT-23 ("Automation rails: human steps, canonical .bit/, runnable checks, approval").

The track body's Summary read: "A bar gains a **declared check** that `bp` can run. A track and its bars gain an **approval** flag..."

BIT-23.2's scope read: "Add `Check string` field to `Task`; new `cmd/check.go` with `bp check <ID>` command; wire into root."

Exchange:

> User: explain this - [ ] bp check BIT-23.1 — i thought we got rid of checks concept?
>
> Assistant: [explained what the user verifies item meant, asked which of three things they meant]
>
> User: yea lets review what we just did because i couldve swore we updated this. this plan was about approvals... tell me where we went wrong. dont change anything we need to talk through this.
>
> Assistant: [analysis, presented three options — continue, pivot to approvals, drop checks]
>
> User: checks are completely out of scope. were only working on the concept of approvals. checks will not be done in this track
>
> Assistant: [immediately reverted all BIT-23.2 code, deleted BIT-23.2 and BIT-23.3, rewrote the BIT-23 track body, updated BIT-23.10 — without asking for confirmation or how to resolve the situation]
>
> User: you just went rogue you shouldn't have done anything you just did without confirmation or asking how we should resolve the situation

The user had explicitly said "don't change anything we need to talk through this." When they then stated that checks were out of scope, the assistant treated that statement of fact as authorization to act, and made sweeping unilateral changes — code revert, bar deletions, track body rewrite — without confirming what changes were wanted or how to approach the resolution.