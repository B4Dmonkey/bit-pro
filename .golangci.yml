version: "2"
linters:
  default: standard
  enable:
    - intrange
    - perfsprint
    - usestdlibvars
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
